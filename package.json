{
  "name": "meteor-app",
  "private": true,
  "scripts": {
    "start": "meteor run",
    "test-app": "TEST_WATCH=1 meteor test --full-app --driver-package meteortesting:mocha",
    "testcafe": "WEB_PORT=3080 MONGO_PORT=3081 testcafe chrome testcafe/*.testcafe.js --live",
    "testcafe-app": "meteor --port=3080 --settings ../meteor_settings/mc-settings-testcafe.json",
    "unit-test": "meteor --port=3070 test --once --driver-package meteortesting:mocha",
    "visualize": "meteor --production --extra-packages bundle-visualizer"
  },
  "dependencies": {
    "@babel/runtime": "^7.20.7",
    "meteor-node-stubs": "^1.2.5",
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  },
  "meteor": {
    "mainModule": {
      "client": "client/main.jsx",
      "server": "server/main.js"
    },
    "testModule": "tests/main.js"
  },
  "devDependencies": {
    "testcafe": "^3.7.0"
  }
}